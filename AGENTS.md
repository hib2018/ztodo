# AGENTS.md

## プロジェクト概要

`ztodo` は、GitHub Issue を実行可能な Task に分解し、CLI/TUI で管理する小さな Zig アプリケーションです。

- 対象 Zig バージョンは `build.zig.zon` の `minimum_zig_version` を正とする（現在は Zig 0.16.0）。
- GitHub の認証と取得は `gh`、Task 分解は利用者が選んだ AI、クリップボード操作は OS のコマンドへ委譲する。
- 秘密情報をアプリの設定、データ、README、テスト fixture に保存しない。
- ユーザー向けの仕様は `README.md`、TUI の操作仕様は `docs/tui-design.md` を参照する。

## 変更時の基本方針

- 変更は依頼された範囲に絞り、既存の CLI/TUI の挙動と保存形式を不用意に変えない。
- 新しい依存を増やす前に、Zig 標準ライブラリまたは既存の外部コマンドで実現できるか検討する。
- 利用者のデータを最優先で守る。入力を完全に検証してからメモリ上の状態を変更し、保存に成功してから破壊的な後処理を行う。
- ファイル更新は Atomic に行う。途中失敗で既存ファイルを壊したり、Proposal を失ったりしないようにする。
- 削除や承認などの破壊的操作は、既存 UI の方針どおりキャンセルを既定とし、小文字の `y` のときだけ確定する。完了切替や選択移動など、容易に戻せる操作には不要な確認を加えない。
- 既存データの後方互換を保つ。保存スキーマを変える場合は `schema_version`、旧データの読み込み、再保存後の形式を明示的に検討する。
- CLI と TUI が同じドメイン操作や保存処理を使える場合は共有し、画面固有のロジックへ重複実装しない。

## コード構成

- `src/core/`: Task、保存、パス解決などの中核ロジック
- `src/proposal/`: Proposal のモデル、編集、保存、取り込み、Task への適用
- `src/integrations/github/`: `gh` を使う GitHub 連携、設定、Issue 選択、AI 向けプロンプト
- `src/platform/`: OS ごとの差異を吸収する処理
- `src/tui/`: TUI の状態、描画、入力処理
- `src/cli.zig`: 引数解析、各機能のオーケストレーション、利用者向けエラーへの変換
- `src/main.zig`: 最小限のエントリーポイント
- `src/root.zig`: モジュールの公開とテストへの組み込み

新しい処理は責務に合う既存モジュールへ置く。`main.zig` を厚くせず、ドメインロジックを CLI/TUI の入出力処理から分離する。新しいモジュールを追加した場合は、必要に応じて `src/root.zig` から公開し、ルートの test block に含める。

## Zig 実装規約

- 既存コードの Zig 0.16 API とスタイルに合わせ、変更後は `zig fmt` を通す。
- Allocator と `std.Io` は呼び出し元から明示的に渡し、テスト可能性を保つ。
- 所有権を明確にする。確保した値には対応する `defer` / `errdefer` / `deinit` を置き、部分初期化の失敗でもリークさせない。
- 所有する構造体の `deinit` は内部の確保領域を解放し、既存コードに合わせて最後に `self.* = undefined` とする。
- 外部コマンド、JSON、ファイル I/O の詳細エラーは境界でアプリ固有のエラーへ変換し、CLI/TUI では利用者が対処できる文言にする。
- 入力は trim、空文字、UTF-8、制御文字、件数、長さ、範囲を必要に応じて検証する。文字数と Terminal 表示幅を byte 長だけで判断しない。
- 順序と不変 ID を混同しない。画面上の位置は one-based、内部 index は zero-based であることを境界で明確にする。
- コメントは「何をしているか」の言い換えではなく、互換性、安全性、非自明な判断理由を説明する場合に限る。

## テスト

- 実装を変更したら、正常系だけでなく、境界値、無効入力、途中失敗、既存状態が変わらないことも同じ変更でテストする。
- テストは原則として対象実装と同じ `.zig` ファイルに置き、観測する振る舞いが分かる英語名を付ける。
- メモリを所有する処理は `std.testing.allocator` を使い、リークと解放漏れを検出できるようにする。
- 永続化では JSON round trip、Unicode、順序、ID、schema、旧形式、壊れた入力、Atomic 置換を確認する。
- TUI では状態遷移に加え、キー入力、キャンセル既定、狭い Terminal、長い Unicode、選択位置、保存結果を確認する。
- GitHub やクリップボード連携の単体テストで、実ネットワーク、実認証、利用者のクリップボードに依存しない。

変更後の基本確認:

```sh
zig fmt build.zig src
zig build test
zig build
```

環境制約で実行できない確認がある場合は、未実行のコマンドと理由を報告する。

## ドキュメントとリリース整合性

- コマンド、キー操作、必要環境、保存場所、外部コマンド、ユーザー向けメッセージを変えた場合は `README.md` を同じ変更で更新する。
- TUI の画面構成や操作規則を変えた場合は `docs/tui-design.md` を先に、または実装と同時に更新する。
- CLI コマンドを追加・変更した場合は help text、引数解析テスト、`extras/zsh/completions/_ztodo` も確認する。
- バージョンを変更する場合は `build.zig.zon` を唯一の値の定義元として扱い、README、タグ、リリース内容との整合を確認する。

## 完了条件

- 依頼された CLI/TUI の振る舞いが実装されている。
- 失敗時に既存の Task、Proposal、設定を壊さない。
- 関連するテストとドキュメントが更新されている。
- `zig fmt`、`zig build test`、`zig build` の結果を確認している。
- 変更内容、テスト結果、残る制約を簡潔に報告している。
